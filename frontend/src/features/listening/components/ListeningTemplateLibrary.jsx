// ListeningTemplateLibrary removed to reduce bundle size and simplify editor.
// Templates were removed by request — keep a minimal stub so imports remain valid.
import React from 'react';

const ListeningTemplateLibrary = () => null;

export default ListeningTemplateLibrary;
